#include <iostream>

/*
  	1. nullptr solves:
	void	f(void *)
	void	f(int x)

	f(nullptr)
	std::nullptr_t can be casted to any pointer;

	2. enum class:
	enum class Enum1 {a,b,c}
	need to use :: semantics

	3. explicit operator:
	only implicit conversion

	4. typedef:
	For template classes.

*/

int		main(void)
{


	return (0);
}